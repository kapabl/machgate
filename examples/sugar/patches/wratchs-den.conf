# Binary patches for The Wratch's Den (Sugar engine)
#
# Skip Steam initialization — returns 0 (no Steam) immediately.
# sugar::steam::init_steam at 0x100088158: sub sp → mov w0, #0; ret

0x100088158 = d10183ff d2800000
0x10008815c = a9036ff6 d65f03c0
